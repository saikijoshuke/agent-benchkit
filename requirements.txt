# Agent BenchKit — 调研 Agent 评测框架
# 核心依赖
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-deepseek>=0.1.0
# 检索后端依赖（二选一）：默认走 SerpAPI（基于已安装的 requests，无需额外包）；
# 若 SEARCH_BACKEND=tavily 则需要下面这个：
tavily-python>=0.3.0
pydantic>=2.5.0
python-dotenv>=1.0.0
requests>=2.31.0

# Web UI
gradio>=4.0.0

# 报告导出（PDF）
fpdf2>=2.7.0

# 可选：链路追踪
langfuse>=2.0.0

# 测试 / 校验
pytest>=8.0.0
