# 面试准备

## Q1: LangChain 帮你做了什么

LangChain 帮我做了 LLM 调用、工具注册、消息格式转换，但我没有用 AgentExecutor。

**LangChain 帮我做的：**
- ChatDeepSeek 封装了 API 调用和认证
- bind_tools 把 Python 函数转成 tool schema
- BaseMessage 系列做消息标准化
- StateGraph 提供图执行引擎

**但我没有用 AgentExecutor：**
- prompt 藏在抽象层里，无法精确控制每轮 system prompt
- 思考-行动-观察循环是固定的，无法插入并行搜索 / 可信度评估 / 反思
- 调试困难，无法在不改源码的情况下插入 tracing

**如果不用 LangChain：**
直接用 httpx + Pydantic 自己实现，多写约 400 行，换来完全可控的执行流。
## Q2: 出错怎么办

**任务拆解错误（3 层降级）：**
1. JSON 解析失败 -> 回灌错误信息重试（最多 2 次）
2. 重试仍失败 -> 兜底：整个主题作为一个子问题
3. 连续搜索不到结果 -> 输出当前信息不足以完成调研

**搜索结果为垃圾（4 层过滤）：**
1. 每条结果截取前 300 字，丢弃低质量尾部
2. LLM 评估来源可信度（官方 > 媒体 > 博客 > 论坛）
3. 低可信来源仅做补充，不进核心分析
4. v4 反思循环检查无引用支撑的断言

**真实案例：** 评测发现 A01 的 5 个 URL 全部验证失败。这说明 LLM 倾向于编造引用，可信度加权和引用校验可以有效控制这个问题。
## Q3: 怎么知道 Agent 变好了

靠 30 道测试题 x 5 项指标 x 4 种架构的对比评测。

**评测链路：**
测试集 (30题) -> Agent 逐题执行 -> 收集报告 -> 引用校验 + 事实匹配 + 要点匹配 + 幻觉检测 -> 输出对比表

**设计原则：**
- B 类覆盖度题 -> 考察广度（有没有遗漏关键维度）
- C 类陷阱题 -> 考察诚实度（不知道时会不会承认）
- 引用真实性 -> 考察严谨性（引用是不是真的）

**解读对比表：**
- v2 耗时降为 v1 的 1/3 且准确率不变 -> 并行是纯收益
- v3 引用真实性显著高于 v1 -> 可信度加权有效
- v4 耗时翻倍但幻觉率没降 -> 反思循环代价大于收益，默认关闭

**迭代流程：**
改 Prompt/架构 -> 跑 5 题验证 -> 看指标 -> 收敛后再跑全量 30 题。靠数字说话，不靠感觉。

## 面试官可能追问

Q: 30 道题自己编的还是公开数据集？
A: 自己编的。好处是精确控制难度和维度，坏处是有主观偏差。下一步引入 MMLU 和 HotpotQA。

Q: 引用校验怎么处理动态页面？
A: 目前用 requests 扫 HTML，SPA 抓不到。这是已知缺陷（README 有列出），生产级需要换 Playwright。

Q: 四个架构哪个最好？
A: 看场景。v2 性价比最高（覆盖 70% 场景），v4 的反思循环只在需要极致严谨时才值得开。